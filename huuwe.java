import java.util.HashMap;

public class huuwe {

    static void removeImage(HashMap <Car, Point> carposition, Car car){
        carposition.remove(car);
    }


    // CarShop<Car> workshop = CarShopFactory(4, new Point(500, 0));
}
